include ./mixins/page-nav-panner.pug 

doctype html
html(lang="ar" dir="rtl" x-data="{ lang: localStorage.getItem('lang') || 'ar', dir: localStorage.getItem('dir') || 'rtl', toggleDirLang() { this.lang = this.lang === 'en' ? 'ar' : 'en'; this.dir = this.dir === 'ltr' ? 'rtl' : 'ltr'; this.updateAttributes(); window.location.reload(); }, setToLTR() { this.lang = 'en'; this.dir = 'ltr'; this.updateAttributes(); window.location.reload(); }, setToRTL() { this.lang = 'ar'; this.dir = 'rtl'; this.updateAttributes(); window.location.reload(); }, updateAttributes() { localStorage.setItem('dir', this.dir); localStorage.setItem('lang', this.lang); document.documentElement.dir = this.dir; document.documentElement.lang = this.lang; }}", x-init="()=> { document.documentElement.lang = lang; document.documentElement.dir = dir; }")
	include ./components/head
	body( x-data="{sideBar:false}")
		include ./components/header
		+page-nav-panner('اختبار تحديد المستوى')
		main.container.grid.gap-16.py-24
			.bg-gray-50.border.border-neutral-200.grid.gap-8.py-6.px-4(class='mx-auto w-full max-w-[991px]  rounded-[32px] ')
				.text-center.text-neutral-900.text-2xl.font-bold.capitalize.leading-normal مرحبًا بك في اختبار تحديد المستوى v5.01
				.cent.gap-8.flex-wrap
					.bg-white.cent_bet(class='flex-1 drop-shadow-sm rounded-[20px] p-4')
						.grid.gap-3
							div(class="text-right text-neutral-900 text-base font-bold  leading-none") 20 دقيقة
							div( class="text-right text-neutral-500 text-sm font-normal  leading-[14px]") المدة الزمنية
						img(src="/dist/assets/user-exam.svg", alt="" class="size-14")
					.bg-white.cent_bet(class='flex-1 drop-shadow-sm rounded-[20px] p-4')
						.grid.gap-3
							div(class="text-right text-neutral-900 text-base font-bold  leading-none") 20 دقيقة
							div( class="text-right text-neutral-500 text-sm font-normal  leading-[14px]") المدة الزمنية
						img(src="/dist/assets/time-exam.svg", alt="" class="size-14")
				div(class="text-neutral-900 text-xl   capitalize leading-7 font-semibold") هذا الاختبار يعتبر وسيلة هام لتحديد مستواك التعليمي
				div(class="text-neutral-500 text-lg font-normal  capitalize leading-7") لذا ارجو الانتباه للنقاط التالية :
					br
					br
					ul.grid.gap-2.list-disc.px-5
						li قبل بدء الاختبار تأكد من استعدادك لاداء الاختبار وتهيئة الظروف المناسبة
						li  تأكد من كفاءة الانترنت اثناء الاختبار
						li  اجب فقط على الاسئلة تعرف اجاباتها ولا تجب على الاسئلة بطريقة عشوائية لان ذلك قد يؤثر على دقة تحديد المستوى 
						li  اذا كنت متأكد من الاجابة اختار الاجابة
						li  اذا كنت غير متأكد( نخطى السؤال )
						li قبل بدء الاختبار تأكد من استعدادك لاداء الاختبار وتهيئة الظروف المناسبة
					br
					| شكرا على اهتمامك



		include ./components/footer
		include ./components/scripts
